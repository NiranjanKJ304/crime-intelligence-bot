[project]
name = "crime-bot-backend"
version = "0.1.0"
description = "Crime Analytics Backend with ETL Pipeline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
